# URL pública do backend (Oracle). Deixe vazio para usar o mesmo host (mesma origem).
# Public backend URL (Oracle). Leave empty to use the same host (same origin).
VITE_API_BASE_URL=https://api.eduassets.tech

# Leave absent/false in the public demo — there is no hosted RFID hardware
VITE_RFID_BRIDGE_ENABLED=false